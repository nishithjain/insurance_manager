package com.insurance.mobile.navigation

/**
 * Root graph: startup → server setup or main shell.
 */
object RootRoute {
    const val Startup = "root/startup"
    const val ServerSetup = "root/server_setup"
    const val Main = "root/main"
}
